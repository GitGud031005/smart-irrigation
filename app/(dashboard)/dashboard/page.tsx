// Dashboard Screen (Mockup 4.2)
// Left column: parameter cards (temp, humidity, soil moisture), manual irrigation control panel
// Right column: temperature chart, moisture chart, zone status bars
// Filters: day/week, zone selection, reset button
export default function DashboardPage() {
  return <div>Dashboard Page</div>;
}
